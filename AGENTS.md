# CubeKorean 项目协作规则

本项目遵循 ADOS：优先最小、可验证、可回滚的修改；修改前搜索现有实现，不为设计模式引入无收益的抽象；代码变化同步更新受影响文档。

## 项目特有规则

- 核心目标是韩语拼写训练，视觉功能不得削弱输入准确性、即时反馈和可访问性。
- 韩语输入判断必须兼容 IME 组合中间态，例如 `ㅋ → 커 → 커ㅍ → 커피` 不得提前判错。
- 课程数据、拼写规则、学习会话和页面展示应保持职责分离。
- MVP 使用本机存储；引入账号、数据库或服务端状态前必须记录架构决策。
- 修改拼写规则必须覆盖基础音节、收音、复合元音、插入、删除和中间位置修改。
- 修改运行入口、核心链路、依赖或测试命令时，同步检查 `docs/PROJECT_ONBOARDING.md`。
- 每次修改运行相关最小测试和 `npm run build`；无法验证时如实记录。

## ADOS Docstring规则

若项目后续加入 Python 服务，所有公共和私有函数都必须使用中文 Docstring 说明真实业务作用，避免占位注释。20～150个可见字符是 Review 标准，语义完整性优先，详细规则见 ADOS `standards/docstrings-and-comments.md`。

TypeScript函数和复杂业务代码同样要求以命名优先；仅在规则、原因或边界无法由代码直接表达时添加有效注释，不复述代码表面动作。
